language: go
go: 1.8.3
install:
  - make get
script:
  - make test
after_success:
  - git config user.email "arran4+travis@gmail.com"
  - git config user.name "Arran Ubels (Travis)"
  - make release
notifications:
  email: false
